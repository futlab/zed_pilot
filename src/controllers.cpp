#include <cmath>
#include "controllers.h"

/*
YawController::YawController() : state_(0)
{

}

float YawController::calcAngularZ(float a) {
    if (a > 0.5) a = 0.5;
    else if (a < -0.5) a = -0.5;
    if (fabs(a) < 0.02) {
        state_ += 3;
        if (state_ > 100) state_ = 100;
    } else {
        state_ -= 5;
        if (state_ < 0) state_ = 0;
    }
    return a;
}


YController::YController() : state_(0)
{

}

float YController::calcLinearY(float a)
{

}
*/
